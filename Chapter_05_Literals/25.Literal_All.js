let count = 34;
let negative = -100;
let zero = 0;

let h = 0xFF; // hexadecimal literal (255 in decimal)
console.log(typeof h);
let color_hex = 0xFF0000; // hexadecimal literal for red color

let octal = 0o755; //base octal literal (493 in decimal)

let million = 1e6; // scientific notation for 1 million
let small = 1e-6;
let tiny = 1e-10; 